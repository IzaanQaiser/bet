[project]
name = "extractor-svc"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "google-adk>=2.7.1",
    "obligation-engine-shared",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx2",
]

[tool.uv.sources]
obligation-engine-shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/extractor_svc"]

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"
