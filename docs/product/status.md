# Build Status

Read this after the PRD at the start of every session — it's the fast answer to "where are we." Update it whenever a build-order step completes, starts, or gets blocked. This is a living tracker, not a history — keep it short and current; git log is the historical record.

**Last updated:** 2026-08-24 (session 6)

---

## Phase

**Implementation started — on the `dev` branch, working on Step 1.**

All six architecture docs (`docs/architecture/`), all ADRs (`docs/decisions/`), engineering conventions (`docs/engineering/conventions.md`), and the PRD (`docs/product/prd.md`) are written, cross-referenced, and internally consistent as of a full cohesiveness pass on 2026-08-22 (found and fixed one real bug: `due_at` had nowhere to be staged pre-commit — see `docs/architecture/data-model.md` §2.4). PRD §14's build order was subsequently split from 12 steps into 19 smaller, single-concern ones, each with a minimal "Reads" doc list and a checkable "Done when" signal.

## Current step — PRD §14 build order

**Steps 1-15 done. Phase F (ship) paused — user live-tested the system and redirected toward a conversational UX redesign first (Phase G, in progress). Plan: `/Users/izaan/.claude/plans/spicy-purring-karp.md`.**

| Step | Status |
|---|---|
| **Phase A — Foundation** | |
| 1. Infra skeleton (Terraform) | **Done** — applied to `obligation-engine-hack`, all acceptance criteria verified (idempotent, IAM scoping confirmed, resource inventory confirmed) |
| 2. DB schema + shared package | **Done** — migration applied, all 11 tests pass (8 unit + 3 integration, run for real against live Cloud SQL) |
| 3. `ingest-svc` + real Twilio number | **Done** — deployed to Cloud Run, real Twilio number (`+14152365420`) wired to it, a real SMS from the developer's phone was confirmed end-to-end in the database (`state='RECEIVED'`, correct `user_id`). Two real deploy-time bugs found and fixed — see Notes. |
| **Phase B — Core pipeline (auto-confirm stub)** | |
| 4. `extractor-svc` | **Done** — deployed to Cloud Run, real end-to-end verified: a real `RawItemMessage` published to `items-raw` produced a correct `ExtractedItemMessage` on `items-extracted` via the real Gemini 3.5 Flash call, `type="obligation"`, `due_at` correctly left null for ambiguous "Friday", `effort_minutes=15` (int). Three real deploy-time bugs found and fixed — see Notes. |
| 5. `resolver-svc` stub (temporary, auto-confirm) | **Done** — deployed to Cloud Run, real end-to-end verified against the live DB: an `ExtractedItemMessage` with empty `missing_fields` produced a `CONFIRMED` `items` row (all extracted fields correctly written) and a matching `ConfirmedItemMessage` on `items.confirmed` with `action_type="calendar"`. |
| 6. `committer-svc` | **Done** — deployed to Cloud Run, real end-to-end verified against the live Calendar API: a `ConfirmedItemMessage` produced a real Google Calendar event (read back via the API to confirm — title, `start`/`end` correct, `status="confirmed"`), a matching `obligations` row with the real `calendar_event_id`, and `items.state='COMMITTED'`. Required a one-time manual GCP Console OAuth bootstrap — see Notes. |
| **Phase C — The differentiator** | |
| 7. Capacity engine, pure functions | **Done** — `services/dispatcher-svc/src/dispatcher_svc/capacity_engine.py`, 25 tests, all passing, no I/O. Worked example reproduced exactly (`fit_score=0.875`, `revival_score≈0.633`), contrast example reproduces `fit_score=0`. |
| 8. `dispatcher-svc` | **Done** — deployed to Cloud Run, Cloud Scheduler jobs live, real end-to-end verified against the real Calendar and real Twilio: 7 `capacity_snapshots` persisted, a real reminder SMS sent and confirmed idempotent (second run sent 0), a real suggestion SMS sent matching `agent-contracts.md` §4.2 exactly with a real `suggestions` row + `latents` update. Several real deploy-time bugs found and fixed — see Notes. |
| **Phase D — Trust and quality features** | |
| 9. Real `resolver-svc` — confirmation | **Done** — deployed to Cloud Run (`resolver-svc` + `ingest-svc`), real end-to-end verified for both `Y` and `N`: a real confirmation card SMS sent, a real signed webhook reply routed through `ingest-svc` → `resolver-svc` → `items.confirmed` → `committer-svc` → a real Calendar event (`Y` path), and a real `CANCELLED` state + "Cancelled." SMS (`N` path). Also built `ingest-svc`'s inbound-reply routing (state-machine.md §4), a hard prerequisite not named in this step's one-line description. |
| 10. Real `resolver-svc` — clarification loop | **Done** — deployed to Cloud Run, real end-to-end verified: a real ambiguous obligation ("deadline unclear") got a real clarifying question, a real ambiguous reply ("soon") correctly triggered a real follow-up question (`exchange_count` → 2), and a real clear reply ("this friday at 2pm") correctly resolved to the actual next Friday, `AWAITING_CONFIRMATION`, and a real confirmation card. A real Vertex AI schema limitation (`dict[str, Any]` output fields) was found and worked around before ever deploying — see Notes. |
| 11. Multimodal ingest | **Done** — deployed to Cloud Run (`ingest-svc` + `extractor-svc`), real end-to-end verified with an actual photo MMS'd to +14152365420: real GCS storage, real Gemini multimodal extraction (`title="Pay rent"`, `summary="Pay rent by Friday."`), correctly routed into the real clarification loop since "Friday" is ambiguous, real clarifying SMS delivered and received. A real Pub/Sub non-idempotency finding surfaced along the way — see Notes. |
| 12. Dedupe via embeddings | **Done** — deployed to Cloud Run (`resolver-svc` + `ingest-svc`), real end-to-end verified via a real 3-message signed-webhook sequence: a differently-worded near-duplicate scored 0.8875 similarity (below the 0.92 threshold, correctly no dedupe action) and proceeded normally; a near-literal resend scored high enough to trigger a real `DUPLICATE_SUSPECTED`, a real "Is this the same as X?" SMS, and a real `Y` reply → `MERGED` with no new `obligations` row and the original item left untouched. Real integration tests against actual pgvector also pass. A real routing gap in `ingest-svc` (missing `DUPLICATE_SUSPECTED`) was found and fixed — see Notes. |
| **Phase E — Resilience and polish** | |
| 13. DLQ + error handling | **Done** — deployed to Cloud Run (`committer-svc` + `resolver-svc`), real end-to-end verified: a forced technical failure published straight to the live `items-extracted` topic correctly reached exactly 5 real delivery attempts, forwarded to the real `.dlq` topic, and landed as a real `dead_letters` row with `items.state='FAILED'`; `scripts/replay_dead_letter.py` correctly republished it to `items-extracted` (not `items-raw`) and it failed reproducibly the same way. Two real bugs found and fixed along the way — see Notes, including one found by this step's own verification process, not by writing tests. |
| 14. Feedback loop / dismissal scoring | **Done** — deployed to Cloud Run (`dispatcher-svc` + `ingest-svc` + `committer-svc`), real end-to-end verified: a real `Y` reply produced a real Calendar event and flipped a latent to a committed obligation, a real `N` incremented `dismissal_count`, a real `Later` set `dormant_until = now()+7d` — all via real signed webhooks routed live through `ingest-svc` → `dispatcher-svc`. Two real bugs found in the process (both in step 13's idempotency guard, not new step-14 code) and a real DB grant migration — see Notes. |
| 15. Email draft + send action (stretch) | **Done** — deployed to Cloud Run (`extractor-svc` + `resolver-svc` + `committer-svc`), real end-to-end verified: a real self-addressed SMS was classified `action_type="email"`, drafted a real body, showed the real draft on a real confirmation card, and a real `Y` reply triggered a real Gmail send (`obligations.email_sent_at` set, no exception from the real API call). Spec written before any code (agent-contracts.md §2.1/§3.2/§3.3, state-machine.md §1.5), resolving the "Open gap, flagged rather than invented" note carried since step 9. Found and fixed a real Gemini prompt gap and a real, systemic Pub/Sub ack-deadline bug along the way — see Notes. |
| **Phase F — Ship** | |
| 16. Seed demo data script | Not started — paused for Phase G |
| 17. Record demo | Not started — paused for Phase G |
| 18. README, diagram export, write-up | Not started — paused for Phase G |
| 19. Bonus (blog, social, Veo, Lyria, real onboarding) | Not started — paused for Phase G |
| **Phase G — Conversational redesign (new, user-directed)** | |
| A. Latency fix (`--min-instances=1`) | **Done** — see Notes |
| B. Chat detection (`is_actionable`/`chat_reply`, new `CHATTED` state) | **Done** — see Notes |
| C. `messages` table (tone-mirroring plumbing) | **Done** — see Notes |
| D. Unified conversational turn (resolver-svc) — replaces `clarification.py`/`templates.py`/`reply_classifier.py` for the main confirm flow | **Done** — see Notes |
| Phase 2 follow-up — dispatcher-svc suggestion flow gets the same treatment | Not started (explicit follow-up, not in this pass) |

## Blockers

None.

## Decided

- Account: `waslyrideshare@gmail.com`. Project: `obligation-engine-hack` (plain `obligation-engine` was already taken globally).
- Billing account `01153A-78309A-856476` linked and enabled. (History: was closed due to a declined card; user paid the balance and reopened it; hit a billing-account project-quota limit next, resolved by unlinking `msa-gpt` to free a slot.)
- `obligation-engine-db` (Cloud SQL Postgres 15, `db-f1-micro`) is live at connection name `obligation-engine-hack:us-central1:obligation-engine-db`.
- Media bucket: `obligation-engine-hack-media`. Artifact Registry: `us-central1-docker.pkg.dev/obligation-engine-hack/obligation-engine`.
- Twilio: Account SID `AC3292d4a7944b87b2fe3db562856e32bd`; Auth Token in Secret Manager (`twilio-auth-token`); an API Key's secret also captured and stored (`twilio-api-key-secret`) for outbound sends in later steps — see `infrastructure.md` §4.1 for why there are two separate Twilio credentials, not one.
- Twilio account was **upgraded to paid** (not trial) — a $20 balance was added, and full A2P 10DLC brand + campaign registration was completed (Brand "Izaan Qaiser", Sole Proprietor). Real owned number: **+14152365420** (415/SF area code, Local, SMS+MMS+Voice, $1.15/mo), webhook configured to `https://ingest-svc-ns4t52sm7a-uc.a.run.app/webhook/sms`. Public compliance docs (privacy policy, terms, opt-in page — required for A2P campaign approval) are hosted at https://gist.github.com/IzaanQaiser/ee4ef5c5e3f1320287358b021f8b920f (a public gist, deliberately separate from the private project repo).
- Demo user bootstrapped in `users` table: `+16477401694`, `timezone='America/Toronto'`, default working hours.

## Notes for the next session

- Local toolchain: `uv`, `ruff`, `psql` (via `libpq`), `cloud-sql-proxy` all installed (`$HOME/.local/bin` and `/opt/homebrew/opt/libpq/bin` added to PATH in `~/.zshrc`).
- `migrations/0001_init.sql` applied to the real `obligation_engine` database (schema + table-level GRANTs for the four service IAM users). `shared/obligation_engine_shared` (schemas, db, pubsub helpers) built and tested — `uv run pytest shared/tests/` passes 11/11 when a Cloud SQL Auth Proxy is running (`DB_USER`, `DB_HOST`, `DB_PORT`, `GCP_PROJECT_ID`, `CLOUD_SQL_INSTANCE` env vars — see `shared/tests/test_migration.py` docstring); the 3 integration tests skip cleanly without it.
- Real finding worth knowing: Cloud SQL's `cloudsqlsuperuser` role does **not** include `CREATEDB` — scratch-database create/drop for tests goes through `gcloud sql databases`, not raw SQL. Documented in `infrastructure.md` §2.2 and the test file itself.
- Migration/admin access: run as the developer's own IAM identity (`waslyrideshare@gmail.com`, granted `cloudsqlsuperuser`) through the proxy — never as any of the four service accounts. See `infrastructure.md` §2.2's "Migration/admin bootstrap" note.
- `services/ingest-svc` exists: FastAPI app, real Twilio `RequestValidator`-based signature check, lookup-or-reject on `users.phone_e164` (onboarding is deferred — see below), publishes to `items-raw`. Dockerfile builds and runs correctly — two real multi-stage-build gotchas fixed along the way: (1) the `uvicorn` console script's shebang hardcodes the builder stage's venv path, fixed by invoking `python -m uvicorn` instead; (2) `uv sync`'s default editable install of workspace packages (`shared`) points back to the builder's source tree, which doesn't exist at runtime — fixed with `--no-editable`. Both are worth remembering for every other service's Dockerfile.
- Local Pub/Sub emulator installed (`gcloud components install pubsub-emulator` + `beta`) and OpenJDK (needed to run it) — both via Homebrew, PATH persisted in `~/.zshrc`. `scripts/setup-emulator.sh` creates the 6 topics against it, mirroring `infra/pubsub.tf`.
- `scripts/migrate.sh` rewritten to track applied migrations in a `schema_migrations` table — it used to blindly reapply every file every time, which fails once a table exists. Real bug found via this: `items.type` was `NOT NULL`, impossible for `ingest-svc` to satisfy since type is unknown until `EXTRACTED`. Fixed via `migrations/0002_items_type_nullable.sql`.
- Cloud SQL Auth Proxy + Pub/Sub emulator were left running in the background at the end of this session (ports 5433 and 8085) — may need restarting in a fresh session/terminal.
- **Two real deploy-time bugs found getting `ingest-svc` actually working in Cloud Run, both worth remembering for every later service:**
  1. `roles/cloudsql.client` alone does **not** grant IAM database authentication — the separate `roles/cloudsql.instanceUser` role is what actually authorizes connecting as a specific IAM DB user. Local testing never caught this because it ran as the developer's own Owner-level identity, which bypasses the check. Added to all four service accounts in `infra/cloud_sql.tf`.
  2. Cloud Run's native `--add-cloudsql-instances` Unix socket does **not** transparently inject an IAM token the way the standalone `cloud-sql-proxy --auto-iam-authn` does for local dev — the application itself has to fetch a real OAuth token (scope `sqlservice.admin`) and use it as the password. `shared/obligation_engine_shared/db.py` now branches on this explicitly.
  3. (Smaller) No service had been granted `SELECT` on `users` at all — added to all four via `migrations/0003_grant_users_select.sql`, since every service will need it eventually (timezone, working hours, refresh token ref).
- `scripts/deploy.sh` now exists (per `infrastructure.md` §6) — `./scripts/deploy.sh <service-name>` builds (with `--platform linux/amd64`, required on Apple Silicon), pushes, and deploys. `/healthz` had to be renamed to `/health` — `/healthz` specifically collided with something at the Google Frontend layer (returned a generic Google 404 page instead of reaching the container at all); the real functional route (`/webhook/sms`) was never affected.
- Every step now has full acceptance criteria and named unit/integration/manual tests in `docs/engineering/test-plan.md` — read that step's section before starting it, and don't consider a step done until its tests pass, not just its code.
- Onboarding (PRD §10) is deliberately not in the critical path — bootstrap the single demo user's OAuth token and `users` row manually (see PRD §14's scope note) rather than building the real SMS onboarding flow. That flow only happens in step 19, if time allows.
- Demo needs seeded/backdated data (`docs/product/prd.md` §13, "Demo data note" + step 16) — don't leave this until step 17.
- CI (GitHub Actions) deliberately not set up yet — add once step 4–6 produces real code and tests to run against.
- **Step 4 (`extractor-svc`) findings, verified empirically in a scratch venv before writing production code (`/tmp/adk-probe`), both documented in `agent-contracts.md` §2 and `infrastructure.md` §2/§3:**
  1. Vertex AI's structured-output schema only supports **string** enum values — `Literal[15, 30, ...]` (ints) fails schema validation outright. The wire-facing Pydantic model (`_ExtractionResult` in `main.py`) uses `Literal["15", "30", "60", "120", "240"]`; cast to `int` when building the real `ExtractedItemMessage`.
  2. `gemini-3.5-flash` 404s on every regional Vertex AI endpoint tried (`us-central1`, `us-east5`, `us-east1`, `europe-west4`) — it's only served via the **global** endpoint. `VERTEX_LOCATION`/`GOOGLE_CLOUD_LOCATION` must be `global`, confirmed with a real successful `generateContent` call.
  3. ADK's `Event.output` attribute is `None` even with `output_schema` set on the `LlmAgent` — the actual structured JSON text is in `event.content.parts[-1].text` and has to be parsed/validated manually. `main.py`'s `_extract()` does this.
  - Code written: `services/extractor-svc/{main.py,pyproject.toml,Dockerfile}` + 7 passing unit tests (envelope decode, error paths, string→int cast, ADK event parsing — all mocked, no live Gemini calls in CI). `scripts/deploy.sh` extended with an `extractor-svc` case: `--no-allow-unauthenticated`, no `--add-cloudsql-instances` (zero DB access, ADR 0003), and it now also grants the Pub/Sub push service agent `roles/run.invoker` on the service and creates/updates its `items-raw` push subscription (with DLQ policy + the publisher/subscriber IAM the DLQ policy needs) — all imperative, matching `pubsub.tf`'s existing "not here" comment.
  - **Doc-hygiene fix, found while wiring this up:** `infrastructure.md` §6 and `infra/main.tf`'s header comment referenced a `cloud_run.tf` that was never actually created — Cloud Run services, their `run.invoker` bindings, and their push subscriptions are all created imperatively in `scripts/deploy.sh`, not Terraform (they need each other in sequence: service → URL → subscription). Docs corrected to match reality; no code behavior changed.
  - **Real deploy-time bugs found and fixed, both worth remembering for `resolver-svc`/`committer-svc` (steps 5–6, same push-subscription pattern):**
    1. Pub/Sub's dead-letter policy has a real minimum of `max_delivery_attempts=5` — the planned `3` was rejected outright by the API. Fixed in `scripts/deploy.sh` and both places in `docs/` that assumed `3` (`infrastructure.md` §1, `test-plan.md` step 13).
    2. The project's Pub/Sub push service agent (`service-<PROJECT_NUMBER>@gcp-sa-pubsub.iam.gserviceaccount.com`) didn't exist yet — enabling `pubsub.googleapis.com` doesn't auto-create it. One-time fix: `gcloud beta services identity create --service=pubsub.googleapis.com --project=obligation-engine-hack` (documented in `infrastructure.md` §2.2's bootstrap note, not made Terraform since it needs the `google-beta` provider for one one-time call).
    3. The originally planned "Pub/Sub push service agent only" invoker pattern doesn't actually work that way — the push subscription's OIDC token has to be minted *as the consuming service's own SA* (`sa-extractor`), not as the raw push agent, which needs a `serviceAccountUser`-style grant the developer's Owner account still can't get on a Google-managed agent. Working pattern (now in `scripts/deploy.sh` and `infrastructure.md` §2.1): grant the push agent `roles/iam.serviceAccountTokenCreator` *on* `sa-extractor`, then grant `sa-extractor` `roles/run.invoker` *on* the Cloud Run service.
  - Deployed and verified for real: published a `RawItemMessage` directly to the live `items-raw` topic ("Bro send rent by Friday"), confirmed the correct `ExtractedItemMessage` arrived on `items-extracted` and Cloud Run logs show a clean `200 OK` with no retries.
- **Step 5 (`resolver-svc` stub) — the planned proof-first pattern paid off again:**
  - Code written: `services/resolver-svc/{main.py,pyproject.toml,Dockerfile}` + 8 tests (6 unit, DB/Pub/Sub mocked; 1 real Pub/Sub-emulator integration test; 1 real end-to-end deploy check, not counted in the automated suite). Stub logic exactly matches `test-plan.md` step 5: writes extracted fields to `items` unconditionally (the "progress" write, `state='EXTRACTED'`), and only when `missing_fields` is empty additionally sets `state='CONFIRMED'` and publishes `ConfirmedItemMessage` with `action_type="calendar"` for obligations / `null` for latents, per `agent-contracts.md` §1.
  - `scripts/deploy.sh`'s `extractor-svc` case's push-subscription IAM chain (tokenCreator + run.invoker + DLQ grants + create/update subscription) was refactored into a shared `setup_push_subscription()` function — used by both `extractor-svc` and the new `resolver-svc` case, and ready for `committer-svc` in step 6 without a third copy-paste.
  - **Real deploy-time finding:** IAM grant propagation is not instant — the very first live invocation attempt against the freshly deployed `resolver-svc` 403'd with `lacks {run.routes.invoke} permission` even though `gcloud ... get-iam-policy` already showed the correct bindings. Not a bug; waited ~2–3 minutes and the identical retry succeeded. Documented in `infrastructure.md` §2.1 so it isn't mistaken for a real bug again when deploying `committer-svc`/`dispatcher-svc`.
  - Also fixed a real cross-package dependency-drift flake found while testing this step: `uv sync` run from inside a single service directory (e.g. to add a dev dep) drops every *other* workspace member and root-level dev tool from the shared `.venv` — it only installs that one package's own deps. `pytest-asyncio` and `httpx2` are now declared in the **root** `pyproject.toml`'s dev group (not just individual services'), and `asyncio_mode = "auto"` is set at the root level too (not just `extractor-svc`'s own config) — a root-level `uv sync` now always restores the full set regardless of which directory the last `uv sync`/`uv add` ran from.
  - Deployed and verified for real against the live DB (not the emulator): inserted a real `RECEIVED` items row, published a real `ExtractedItemMessage` to `items-extracted`, confirmed the row flipped to `state='CONFIRMED'` with every extracted field written correctly, and the matching `ConfirmedItemMessage` arrived on `items.confirmed`.
- **Step 6 (`committer-svc`) — the one step so far needing a real manual GCP Console action, PRD §14 anticipated this exactly.** Neither `google-oauth-client-secret` nor the demo user's `google_refresh_token_ref` were ever populated before this step — the PRD's own deliberate scope call (§14: *"bootstrap manually — a refresh token minted once via the OAuth flow directly, dropped into Secret Manager"*), not a gap introduced this session.
  - Code: `services/committer-svc/{main.py,pyproject.toml,Dockerfile}` + 9 tests (7 unit — DB/Secret Manager/Calendar all mocked; 2 real-DB integration tests, Calendar mocked) all passing. `scripts/deploy.sh` extended with a `committer-svc` case (Cloud SQL access, `GOOGLE_OAUTH_CLIENT_ID` as plain env var, `GOOGLE_OAUTH_CLIENT_SECRET` via `--set-secrets`, reuses `setup_push_subscription` for `items.confirmed`).
  - Manual bootstrap done: OAuth consent screen created (app name "bet", External + Testing), `waslyrideshare@gmail.com` added as a test user, scopes `calendar.events`+`gmail.send` added, Desktop-app OAuth client created. Real friction along the way: the app's Branding section had to be completed before the consent screen would work at all ("OAuth configuration is incomplete" warning), and the first consent attempt 403'd with `access_denied` because the test-user grant hadn't been added yet — both are normal one-time GCP Console setup snags, not code bugs.
  - `scripts/bootstrap_oauth_token.py` (new, one-time local script — needs `google-auth-oauthlib`, not a project dependency, run via `uv run --with`) ran the consent flow, minted a refresh token, created `user-refresh-token-{user_id}`, and updated the demo user's row. **Real bug in the script itself, fixed in-session:** `InstalledAppFlow.run_local_server()`'s printed auth URL and the `webbrowser.open()` call were both silently swallowed by Python's default stdout block-buffering when run non-interactively — looked like the script hung with no browser opening. Fixed by running with `python -u`/`PYTHONUNBUFFERED=1`; the auth URL was then visible and could be opened manually.
  - **Resolved gap, documented in `agent-contracts.md` §1:** neither the PRD nor any doc specified how long the written Calendar event actually runs for. Decided: `due_at` to `due_at + effort_minutes` — the block represents the time set aside to do the task, consistent with the capacity engine treating obligations as real occupied time. Also decided: a naive (no-UTC-offset) `due_at` from Gemini is treated as already being in the user's `users.timezone`, not UTC.
  - Deployed and verified for real: same IAM-propagation-delay finding as step 5 hit again (first delivery attempt 403'd, a republish after the grants settled succeeded) — reconfirms `infrastructure.md` §2.1's note rather than being a new issue. The real, live-published `ConfirmedItemMessage` produced an actual Google Calendar event, **read back via the Calendar API** (not just "no error" — `summary`, `start`/`end` spanning exactly `due_at` to `due_at+15min` in `America/Toronto`, `status="confirmed"`), a matching `obligations` row with the real `calendar_event_id`, and `items.state='COMMITTED'`. Test event and DB rows cleaned up afterward.
- **Step 7 (capacity engine) — the cleanest step so far, no real findings, matched the doc exactly.** `services/dispatcher-svc/` scaffolded early (just `capacity_engine.py` + tests, no `main.py`/`Dockerfile`/FastAPI dep yet) — same pattern used for `extractor-svc`/`resolver-svc` ahead of their own build steps. Module lives in `dispatcher_svc`, not `shared`, since dispatcher-svc is its only caller. 25 tests, zero I/O, every formula transcribed directly from `capacity-engine.md`; the worked example (§6) reproduces `fit_score=0.875` and `revival_score≈0.633` to 3 decimal places by hand-verifying the arithmetic before writing any test, and the contrast example correctly reproduces `fit_score=0`. No deploy, nothing to verify manually — `capacity-engine.md` §0 called this "explicitly I/O-free" and it held.
- **Step 8 (`dispatcher-svc`) — the most real-bug-dense step yet, four genuine findings hit deploying against real Calendar/Twilio/Postgres, none hypothetical.** Deliberately scoped to the *send* side only per `test-plan.md`: computes/persists snapshots, sends idempotent reminders, sends at most one suggestion. Parsing a `Y`/`N`/`Later` reply and any `SURFACED`-phase transition (state-machine.md §2.2) is NOT built here — that's a different concern belonging with the feedback-loop step, noted explicitly in `main.py`'s module docstring and `test-plan.md` rather than silently deferred.
  - Code: `services/dispatcher-svc/{main.py,calendar_client.py,templates.py,pyproject.toml,Dockerfile}` + 22 new tests (14 unit — templates pure, DB/Twilio mocked for orchestration; 3 real-DB integration tests, Calendar+Twilio mocked). `scripts/deploy.sh` extended with a `dispatcher-svc` case; `scripts/dispatch-now.sh` written (the manual demo trigger infrastructure.md §5 already specified).
  - **Real design decision, initially built wrong then fixed before any deploy:** first draft fetched Calendar events per-day (21 calls/run) — caught against `infrastructure.md` §4's own stated quota assumption ("two reads per user per run") before ever deploying. `calendar_client.fetch_events_for_range()` now does one bulk range-fetch for the trailing window and one for the forward window, partitioning returned events by day locally (handles multi-day events too).
  - **Resolved gap, documented in `capacity-engine.md` §1:** "next 7 days" and "trailing 14 days ending today" are both inclusive of today — undefined in the doc, decided here since the dispatcher can run at any hour via manual trigger.
  - **Resolved gap, documented in `agent-contracts.md` §4.1:** a reminder fires the first run where `now + reminder_window_hours >= due_at` — also undefined before this step.
  - **Real bug #1, found on the first live `/dispatch` run:** `ZeroDivisionError` in `load_delta` — the demo Calendar's trailing 14 days are genuinely empty (`rolling_mean=0`), not just short-history cold-start (already handled). Fixed: `load_delta=0.0` if today is also empty, else `1.0` (saturates `load_fit`'s floor). Documented in `capacity-engine.md` §3, regression-tested.
  - **Real bug #2:** `permission denied for table capacity_snapshots` — the upsert design (`INSERT ... ON CONFLICT DO UPDATE`, for idempotent re-runs on the same day) needs `UPDATE` privilege, which the original plan only ever granted as `SELECT, INSERT`. Fixed via `migrations/0004_grant_dispatcher_capacity_snapshots_update.sql`, documented in `infrastructure.md` §2.2.
  - **Real bug #3, external to this codebase entirely:** the step-3-era Twilio API Key (`SK3f3f93...`) returned `401 actor doesn't have any assertions` on every send — reproduced with a raw `curl` outside any of this project's code to confirm it wasn't a bug here, then confirmed Account SID+Auth Token sent fine with the identical message, isolating it to that specific key. Fixed by minting a fresh API key via the Twilio REST API, verified working before adopting it, deleted the broken one, rotated `twilio-api-key-secret` to the new value. Not a mystery worth chasing further — likely a stale/half-provisioned key from before A2P campaign approval finished.
  - **Positive finding, no fix needed:** unlike Pub/Sub's push subscriptions (steps 4-6), Cloud Scheduler's OIDC job creation needed no explicit `serviceAccountTokenCreator` grant — `gcloud scheduler jobs create` with `--oidc-service-account-email=sa-dispatcher` just worked.
  - **Doc-hygiene fix, found while wiring this up:** `infrastructure.md` §6 still listed a planned `cloud_scheduler.tf` — same "needs a live Cloud Run URL first" problem as `cloud_run.tf`'s absence (step 4). Scheduler jobs now created imperatively in `scripts/deploy.sh`, docs corrected to match.
  - Deployed and verified for real, twice over: (1) a real obligation → real reminder SMS delivered with exactly correct template text, confirmed idempotent by running `/dispatch` twice; (2) a real 25-day-old latent against the demo user's empty Calendar → real suggestion SMS delivered with exactly correct template text (`"lightest day you've had in two weeks"` evidence branch, correct block/time-of-day/days-since-capture), a `suggestions` row with `outcome IS NULL`, and `latents.surface_count` incremented. All test data cleaned up afterward.
- **Step 9 (real `resolver-svc` confirmation) — bigger than its one-line build-order description, no real bugs this time.** The step's own required manual verification ("one real SMS confirm/cancel round trip") is impossible without the inbound-reply routing (state-machine.md §4) also existing, so that got built here too rather than left for whichever later step happened to need it — documented as a deliberate scope call in `test-plan.md` and `state-machine.md`, not a silent expansion.
  - Code: `shared/obligation_engine_shared/reply_classifier.py` (`classify_reply`, agent-contracts.md §4.3 — one implementation, reused by `dispatcher-svc` too) + `RoutedReplyMessage` schema for the new ingest→resolver call. `resolver-svc` rewritten: `/pubsub/push` now gates on `missing_fields` **and** `confidence >= 0.75` (state-machine.md §1.2) — a complete-but-unsure item is left in `EXTRACTED` exactly like an incomplete one — and a complete/confident item gets a real confirmation card (new `templates.py`, agent-contracts.md §3.3/§3.4) plus a `conversations` row staging `due_at` for later. New `POST /reply` handles `Y` (publish `ConfirmedItemMessage`, built from the `items` row + staged `due_at`) / `N` (`CANCELLED` + "Cancelled." SMS) / anything else (logged, no action — step 10's job, per test-plan.md's explicit scope note).
  - `ingest-svc` now checks for an open conversation before treating a message as new, forwarding to `resolver-svc`'s `/reply` via a synchronous, ID-token-authenticated service-to-service call (`google.oauth2.id_token.fetch_id_token`) — exactly the mechanism `infrastructure.md`'s IAM matrix already anticipated (`sa-ingest` → `run.invoker` on `resolver-svc`) back when it was first written, now actually wired up. The suggestion-reply branch of the same routing table (forward to `dispatcher-svc`) is still not built — noted explicitly in both `ingest-svc/main.py` and `state-machine.md` §4 as a real, currently-unreachable gap (nothing sends a suggestion reply that could hit it yet either), not silently missing.
  - `scripts/deploy.sh`: `resolver-svc`'s case now grants `sa-ingest` `run.invoker` on itself and adds `twilio-api-key-secret`; `ingest-svc`'s case looks up `resolver-svc`'s URL and passes it as `RESOLVER_SVC_URL`.
  - Deployed and verified for real, both directions, through the *actual* deployed services: a real `ExtractedItemMessage` produced a real confirmation card SMS; a real **signed webhook POST straight to the deployed `ingest-svc`** (same signature scheme an actual inbound SMS carries — chosen over physically texting so the full chain could be verified precisely and repeatably, not because a real round trip wasn't required) with `Body="Y"` drove the entire chain for real: routing → forward → `resolver-svc` → `items.confirmed` → `committer-svc` → a real Calendar event, `items.state` walking `RECEIVED → AWAITING_CONFIRMATION → CONFIRMED → COMMITTED`. A second real item verified the `N` path: `Body="N"` → `items.state='CANCELLED'` → real "Cancelled." SMS delivered, no Calendar write, no publish. Test event and all DB rows cleaned up afterward.
- **Step 10 (real `resolver-svc` clarification loop) — a real Vertex AI limitation found and worked around *before* writing any production code, matching the established "verify empirically first" pattern.** A scratch test of `agent-contracts.md` §3.2's exact spec (`ClarificationResponse.filled_fields: dict[str, Any]`) reproducibly made Gemini emit a huge run of whitespace padding inside an otherwise-empty object instead of real key-value content — tried `dict[str, str]` too, same failure. Root-caused to the open-key/`Any`-typed dict shape itself, not a prompt issue (a concrete, non-dict schema worked immediately). Since `due_at` is the *only* field the extractor's own contract ever adds to `missing_fields`, the real implementation (`resolver_svc/clarification.py`) uses a narrow `due_at_filled: bool` / `due_at: str | None` schema instead of the generic multi-field design — documented as a "Resolved gap" in `agent-contracts.md` §3.2 and `capacity-engine.md`-adjacent `state-machine.md` §1.2 (the confidence-only-trigger case collapses to the same conclusion: nothing to ask about, so it goes straight to `AWAITING_CONFIRMATION`).
  - Code: `resolver_svc/clarification.py` (new Gemini call, ADK `LlmAgent` pattern reused from `extractor-svc`) + `templates.py`'s new `render_needs_review`. `main.py`'s `/pubsub/push` now starts a real clarification exchange for any item with `missing_fields` (replacing step 9's "left in `EXTRACTED`, do nothing"); `/reply` branches on `items.state` (`CLARIFYING` vs `AWAITING_CONFIRMATION`) to route to the right handler. Exchange counting matches `state-machine.md` §1.2 exactly: increments only when a question is *sent*, capped at 3, the 4th unresolved reply goes straight to `NEEDS_REVIEW` with no 4th question — verified with a real multi-call sequence against a stateful fake DB connection (`test_exchange_counting_table`), not just isolated single-call mocks, since exchange counting is inherently a sequence property.
  - Still deliberately not built, despite step 9's comment calling it "step 10's job": a correction reply during `AWAITING_CONFIRMATION`. On closer inspection it needs its own field-targeting heuristic (`agent-contracts.md` §3.2's "cheap heuristic" paragraph) that doesn't reuse the due_at-only clarification model cleanly — deferred again, explicitly, rather than bolted on halfway. Noted in `main.py`'s module docstring and `test-plan.md`.
  - Deployed and verified for real: a real obligation with an unclear deadline got a real clarifying question ("When would you like this to be due?"); a real ambiguous reply ("soon") correctly stayed unresolved and triggered a real, sensibly-worded follow-up question, `exchange_count` incrementing to 2 exactly as designed; a real clear reply ("this friday at 2pm") correctly resolved to the actual calendar date (Fri 28 Aug 2026, verified independently) without `exchange_count` incrementing again, landing on `AWAITING_CONFIRMATION` with a correctly-rendered real confirmation card. Test rows cleaned up afterward.
- **Step 11 (multimodal ingest) — the first-try-clean step for the actual multimodal API (no `dict`-schema surprise like step 10's), but real Pub/Sub redelivery behavior surfaced a genuine idempotency gap in step 10's code while verifying it.**
  - Code: `ingest-svc/main.py`'s `_store_media()` downloads the first MMS attachment from Twilio (Basic Auth) and uploads it to `gs://obligation-engine-hack-media/{item_id}.{ext}` **before** the `items` row is inserted — a failed download leaves nothing written for Twilio's retry to duplicate, rather than an orphaned `RECEIVED` row. Only 5 MIME types are accepted (`image/jpeg|png|gif|webp`, `application/pdf` — PRD §1's stated input modes); anything else is a 400 with no DB write, not a silent drop. `extractor_svc/main.py`'s `_download_media()` downloads the `gs://` URI back down and passes it to Gemini as a second `Part` alongside the text `Part` (`types.Part.from_bytes`) — this API worked correctly on the first attempt, unlike step 10's `dict[str, Any]` schema failure.
  - **Real design decision:** only the first attachment is handled if an MMS has more than one — `agent-contracts.md`/PRD §5.1 already establish extraction always produces exactly one item per message regardless, so a second attachment has nowhere structured to go.
  - **Pytest naming collision found and fixed:** `ingest-svc/tests/` and `extractor-svc/tests/` both had identically-named `test_media.py`/`test_media_integration.py`, with neither directory carrying an `__init__.py` (the established project convention) — running the full suite from the repo root collided on module identity. Renamed to `test_ingest_media*.py` / `test_extractor_media*.py`, matching the per-service-prefix convention already used elsewhere (`test_extractor_integration.py`, etc.).
  - Deployed and verified for real via an actual MMS sent from the developer's phone to +14152365420 (a photo of a note reading "Pay rent by Friday"), not a simulated webhook: `items.raw_media_uri` landed as a real `gs://obligation-engine-hack-media/<item_id>.jpg` object, the real Gemini call correctly extracted `type="obligation"`, `title="Pay rent"`, `summary="Pay rent by Friday."`, and — since "Friday" is genuinely ambiguous — correctly fell through to step 10's real clarification loop rather than guessing a date; `conversations.exchange_count=1` confirms exactly one clarifying question was sent, and the developer confirmed receiving it.
  - **Real finding, not a step 11 bug but surfaced by this test:** the same live MMS also triggered 3 duplicate concurrent Pub/Sub redeliveries of the single `items-extracted` message while `resolver-svc` was mid-cold-start (the clarification Gemini call ran long enough to exceed the ack deadline). All 3 duplicates hit `AlreadyExistsError` on ADK's `InMemorySessionService` (deterministic `{item_id}-0` session id isn't safe against concurrent handlers for the same item) and 500'd; harmless here since the winning request had already sent the real SMS before the duplicates arrived, and `extractor-svc` itself only ran once (confirmed via Cloud Run logs — this was pure redelivery of one message, not 4 independently-extracted items). Documented in `test-plan.md`'s step 13 section as a concrete case to cover there, not patched here to avoid scope creep into step 13's own territory.
- **Step 12 (dedupe via embeddings) — no schema/infra changes needed at all (`item_embeddings`, `dedupe_hash`, `DUPLICATE_SUSPECTED`, and `sa-resolver`'s grants on `item_embeddings` were all already provisioned back in step 1), but real testing surfaced one routing gap and one useful calibration data point.**
  - Code: new `resolver_svc/dedupe.py` (`compute_dedupe_hash`, `embed`, `vector_literal`, `cosine_similarity`, `classify_match` — pure functions, no DB access, matching `clarification.py`'s own separation from `main.py`'s DB/orchestration code). `main.py`'s `/pubsub/push` now runs the dedupe check first, before the completeness check, exactly as state-machine.md §1.1 specifies — even an item with `missing_fields` set goes to `DUPLICATE_SUSPECTED` first if a match is found. `templates.py` gained the dedupe question, `MERGED`, and thread-attach-suffix/`Attached to "X".` templates (agent-contracts.md §3.1/§3.3/§3.4).
  - **Real routing gap found and fixed:** `ingest-svc`'s open-conversation check (`_open_conversation_item_id`) only ever listed `CLARIFYING`/`AWAITING_CONFIRMATION` — `DUPLICATE_SUSPECTED` is equally a waits-on-SMS-reply state (state-machine.md's own state reference table already said so) but was never added when it started actually being reachable. Without this fix, a reply to "is this the same as X?" would have been misrouted as a brand-new item. `data-model.md` §2.5's own documentation of this join had the same gap, fixed alongside the code.
  - **Resolved gaps, none in any doc before this step, all now in `data-model.md` §2.7:** (1) an item's own embedding is written to `item_embeddings` at the same point `dedupe_hash` is computed — immediately on entering `EXTRACTED`, regardless of the item's eventual outcome (even a later-`CANCELLED`/`MERGED` item keeps its embedding row and can still surface as a future match) — mirroring `dedupe_hash`'s own "always computed on entry" behavior rather than deferring to `CONFIRMED`/`COMMITTED`. (2) a pending dedupe match or thread-attach candidate has nowhere with a dedicated column to live between being found and being acted on (`items.parent_item_id` is reserved for the *confirmed* thread-attach relationship only) — both ride in `conversations.resolved_fields` under `_dedupe_match_*`/`_thread_attach_*` keys, reusing the existing scratchpad rather than adding new columns.
  - **Implementation note, verified empirically before writing any code (matching the project's established pattern):** `text-embedding-004` works identically at both `us-central1` and `global` — unlike `gemini-3.5-flash`, which only works at `global` (step 4's finding). No `pgvector` Python package is used; `dedupe.py`'s `vector_literal()` formats a plain pgvector text literal and the SQL casts it with `::vector` server-side.
  - Deployed and verified for real via a real 3-message signed-webhook sequence against the live `ingest-svc`/`resolver-svc`: message 1 ("Pay rent by September 4 2026, 2pm - $1450") confirmed to a real Calendar event. Message 2, genuinely differently worded ("hey don't forget the rent is due this friday, need to send $1450"), scored a real similarity of **0.8875** against message 1's embedding — below the 0.92 duplicate threshold, so it correctly proceeded through the normal clarification flow with no dedupe action (useful real calibration data point: two messages clearly about the same underlying fact, worded differently, don't false-positive as duplicates). Message 3, a near-literal resend of message 1's exact text, scored high enough to trigger a real `DUPLICATE_SUSPECTED`, a real `Is this the same as "Pay rent"?` SMS, and a real `Y` reply correctly produced `MERGED` — confirmed via the DB that no `obligations` row was created for the merged item and the original item's `COMMITTED` state was left untouched. The real exact-hash-match path (a genuinely identical resend) is covered by `test_dedupe_integration.py` against real Postgres/pgvector rather than repeated manually, since it needs no embedding call to demonstrate. All test Calendar events and DB rows cleaned up afterward.
- **Step 13 (DLQ + error handling) — the step that most rewarded verifying against real infra instead of trusting the local emulator or a single design pass; two real bugs found, the second one by the verification process itself, not by writing more tests.**
  - **Real finding, reshaped the whole test strategy before any integration test was written:** the local Pub/Sub emulator does not implement push redelivery or dead-letter forwarding at all — confirmed empirically (a message left un-acked for 90s against a 5s ack deadline was delivered exactly once, nothing ever reached the `.dlq` topic). It only enforces `max_delivery_attempts>=5` at subscription-creation time, a materially different and narrower thing than actually running the retry/forward mechanism. Re-verified against real GCP Pub/Sub instead (a throwaway topic, pull mode to avoid needing a public push endpoint): real Pub/Sub forwards after exactly 5 delivery attempts, the forwarded message's `data` is byte-identical to the original, and it carries the real retry count as a `CloudPubSubDeadLetterSourceDeliveryCount` string attribute. This is why `committer-svc`'s new integration tests hit `/pubsub/dlq` directly with a hand-crafted envelope matching that confirmed real shape, and why the full chain is proven in this step's manual/real verification instead of an automated integration test.
  - Code: `committer-svc` gains `POST /pubsub/dlq?stage=<topic>` — decodes the dead-letter envelope (`obligation_engine_shared.pubsub.decode_dead_letter_envelope`, new), writes a `dead_letters` row, sets `items.state='FAILED'`. Three new push subscriptions (one per `.dlq` topic, `scripts/deploy.sh`'s new `setup_dlq_subscription()`), all targeting the same service — `?stage=` on the push endpoint URL is the "which topic did this come from" signal, deliberately not parsed out of Pub/Sub's own dead-letter attributes (simpler, doesn't depend on subscription-naming staying stable). `scripts/replay_dead_letter.py` republishes a stored `payload` verbatim to the topic matching `stage`.
  - **Real bug #1, the one this step was scoped to fix:** step 11's live MMS testing found a concurrent Pub/Sub redelivery racing `resolver-svc`'s clarification start, crashing on ADK's session-id collision — harmless there purely by luck (the winning request had already sent its SMS). Fixed with a real idempotency guard at the top of `/pubsub/push` in both `resolver-svc` and `committer-svc`: skip reprocessing (ack, no-op) anything that's already fully completed.
  - **Real bug #2, found while *verifying* bug #1's fix, not by writing a test for it:** the first draft of `resolver-svc`'s guard checked `items.state != 'RECEIVED'`. A real forced-failure test (a deliberately-invalid `user_id` foreign key on the `conversations` INSERT) showed this was wrong — `_write_item()` commits the `items.state` transition in its own transaction, separate from the later `conversations` INSERT, so the forced failure left a real item stuck at `AWAITING_CONFIRMATION` with zero `conversations` rows. The state-only guard then swallowed every subsequent redelivery as "already done" forever — the message never reached 5 delivery attempts, never reached `dead_letters`, silently defeating the entire mechanism this step exists to build. Fixed by keying the guard on whether a `conversations` row exists instead (documented as the one true completion signal, `data-model.md` §2.4/§2.7) — `committer-svc`'s guard didn't have this problem, since its `obligations`/`latents` INSERT and its `items` state UPDATE already commit together in one transaction. Full writeup, including the one known accepted gap this doesn't close (a real Calendar write succeeding before an unrelated DB failure in `committer-svc` still isn't idempotency-guarded, since the external call can't be made atomic with the local commit), is in `state-machine.md` §3.
  - **Doc-hygiene fix:** `state-machine.md` §3 said Pub/Sub retries "3 attempts" — stale since step 4 found the real minimum is 5; corrected alongside the rest of this section's rewrite. `data-model.md`'s `dead_letters.stage` DDL comment used a dotted example (`'items.extracted'`) that never matched the real dash-named topics (`items-extracted`) — fixed to match reality.
  - Deployed and verified for real, twice over (once against the buggy guard, once against the fix): a forced technical failure published straight to the live `items-extracted` topic against the first (buggy) guard confirmed the exact stuck-forever failure mode predicted above (`AWAITING_CONFIRMATION`, 0 `dead_letters` rows, indefinitely). After redeploying the fix, the identical forced failure correctly reached exactly 5 real delivery attempts, a real `.dlq` forward, a real `dead_letters` row with the correct `item_id`/`stage`/`error`/`retry_count=5`, and `items.state='FAILED'`. `scripts/replay_dead_letter.py` correctly republished the stored payload to `items-extracted` (not `items-raw`, confirming stage-correct replay) and it failed reproducibly the same way, dead-lettering a second time as expected since the underlying bad `user_id` was never actually fixed. All test items/embeddings/conversations/dead-letters cleaned up afterward.
- **Step 14 (feedback loop / dismissal scoring) — the code for `dispatcher-svc`'s own new logic worked correctly on the first real pass; both real bugs found instead lived in step 13's idempotency guard, only reachable once this step's accept path actually exercised it for real.**
  - Code: `dispatcher-svc` gains `POST /reply` (`N`/`Later`/`Y`, state-machine.md §2.2/§2.3) and a 24h no-response timeout (`_resolve_stale_suggestions`, run at the top of `/dispatch`'s per-user loop, before that same run's own scoring — the ordering the acceptance criteria specifically call out). `ingest-svc` completes state-machine.md §4's routing table: an open suggestion (`outcome IS NULL`) now forwards to `dispatcher-svc`, the second branch left unbuilt since step 9 for exactly this reason (no accept-path endpoint existed to forward to).
  - **Resolved gap, found writing the accept path, not discovered by surprise later:** `ConfirmedItemMessage.effort_minutes` is a strict `Literal[15, 30, 60, 120, 240]` (`schemas.py`) — state-machine.md §2.3's "duration = effort_minutes, capped at the block length" can't mean an arbitrary integer. Decided: the largest of the five buckets that's `<=` both the original `effort_minutes` and the real block length, falling back to the smallest bucket if even that overruns — an explicit `Y` always gets scheduled somewhere.
  - **Resolved gap, same reasoning as step 12's dedupe-match staleness:** a suggestion's stored `capacity_snapshots` row can be stale by the time a reply arrives (hours or a day later, real Calendar state can change) — the accept handler re-fetches real, current Calendar events for the suggested day and recomputes the free block at accept time, rather than trusting the stored figure. If the day genuinely filled up since, the suggestion is dismissed with an apologetic message instead of scheduling into a conflict.
  - **Real bug #1, found live-testing the accept path against actually-deployed services (not caught by any mocked unit test, since the mock's default state matched the common case, not this one):** `committer-svc`'s step-13 idempotency guard checked `items.state != 'CONFIRMED'` and silently swallowed the accept — a latent being accepted arrives with `items.state` already `'COMMITTED'` from its *original* commit (a real, legitimate second pass through `/pubsub/push` for the same item, not a redelivery of anything). No error, no `obligations` row, no Calendar event, `suggestions.outcome` left showing `'accepted'` with nothing to show for it. Fixed by keying the guard on whether the row this exact message type would write (`obligations` for an obligation, `latents` for a latent) already exists, rather than on `items.state` — full writeup in `state-machine.md` §3.
  - **Real bug #2, found immediately after deploying bug #1's fix:** `permission denied for table obligations` — `sa-committer` was only ever granted `INSERT` on `obligations`/`latents` (write-only by the original design, since it never needed to read either back before this). Fixed via `migrations/0005_grant_committer_obligations_latents_select.sql`.
  - **Smaller real gap, also found along the way:** `_commit_obligation`'s items UPDATE only ever wrote `state`, never `type` — harmless for every path before this one (a resolver-confirmed item's `type` never changes), but this step's accept path is the first caller that needs a latent's `type` to actually flip to `obligation` on commit. Fixed by always writing `confirmed.type`, a no-op for the pre-existing path.
  - Deployed and verified for real via real signed webhooks to the live `ingest-svc`, routed live to the live `dispatcher-svc`, against two real seeded latents and a real `capacity_snapshots` row from an actual `/dispatch` run (the demo user's calendar was too uniformly empty for the scoring math to naturally clear `REVIVAL_THRESHOLD` on its own — a real, informative finding in itself: a perfectly flat, eventless calendar has no "lighter than usual" signal to detect, so `/dispatch`'s own suggestion-sending was correctly re-verified as real back in step 8 and wasn't the target of this step's testing anyway). First real `Y` attempt reproduced real bug #1 exactly as predicted (silently swallowed, no Calendar event); after the fix, hit real bug #2; after that fix and a real migration, a real `Y` reply produced a real Calendar event (`due_at` correctly at the real block's start), `items.type` flipped from `latent` to `obligation`, and a real `obligations` row. A real `N` reply on a second item incremented `dismissal_count` to 1 with no `dormant_until` (below the dismissal threshold). A real `Later` reply set `dormant_until = now() + 7 days` with `dismissal_count` unchanged. All real Calendar events and DB rows cleaned up afterward.
- **Step 15 (email draft + send action) — the stretch feature the PRD's own cut order named as first to drop if time ran out; reached anyway, spec written first per this step's own test-plan instruction, and the two real findings it produced turned out to matter well beyond this one step.**
  - Spec: `agent-contracts.md` §2.1 (extractor schema/prompt — `action_type`/`email_recipient`/`email_draft` added to the *same* extraction call, no third LLM call site, matching §0's "exactly two call sites" design principle explicitly preserved), §3.2 (the clarification loop's second concrete field, `email_recipient`, extended the same way `due_at` was — not a return to the `dict[str, Any]` shape step 10 already rejected), §3.3 (a third confirmation-card variant showing the full draft, not just a title — the PRD's own "sends the draft back over SMS for review" requirement, which the calendar-obligation card was never built to do). `state-machine.md` §1.5 covers the commit mechanics: no new state, no new OAuth bootstrap (`gmail.send` was already requested alongside `calendar.events` back in step 6, unused until now), and `_already_committed()` (steps 13/14) already generalizes to the email branch with zero email-specific code, satisfying test-plan.md's pre-existing "`email_sent_at` set exactly once" constraint for free.
  - **Resolved design decision, found writing the spec, not obvious from the PRD:** an email action forces `type="obligation"` even with no deadline present — otherwise "email Sarah about the delay" (no date) would classify as a `latent` and get queued into the resurfacing system instead of confirmed immediately, which isn't what a user asking to send something *now* wants. `due_at` stays legitimately nullable for an email obligation (never true for a calendar one) — `ConfirmedItemMessage`/`state-machine.md` §1.5 updated to say so explicitly.
  - **Real bug #1, found in a scratch test against real Vertex AI before ever deploying:** the first prompt draft didn't override the general "obligation implies a deadline" assumption every other rule relies on — a real, fully-specified email obligation with no date mentioned at all still got `due_at` added to `missing_fields`, wrongly asking a clarifying question about a date nobody implied. Fixed with an explicit carve-out in the prompt (`agent-contracts.md` §2.1), reconfirmed on two more real Vertex AI calls (a complete case → `missing_fields=[]`; a name-only recipient case → `missing_fields=["email_recipient"]` alone, not also `due_at`).
  - **Real bug #2, found live-testing the deployed pipeline end to end — and not really a step-15 bug at all, a latent, project-wide gap step 15 happened to be slow enough to trigger for the first time:** a real self-addressed test message hit the exact concurrent-Pub/Sub-redelivery race step 11 first found in `resolver-svc`, except this time in `extractor-svc` — which has **zero** database access (ADR 0003) and therefore cannot have the same kind of idempotency guard `resolver-svc`/`committer-svc` do. Root-caused to something that had been silently wrong since step 1: every push subscription's ack deadline was still Pub/Sub's **default 10 seconds**, never once explicitly set. A real email-drafting extraction call (classify + compose a full body, slower than a plain classify) was consistently slow enough to exceed it, triggering concurrent redeliveries that raced on ADK's session id and burned through enough real delivery attempts to reach `dead_letters` for real — not the "wasteful but harmless, absorbed downstream" case step 11 originally documented. Fixed at the actual source rather than by trying to bolt a guard onto a service that's deliberately not allowed to have one: every push subscription now sets `--ack-deadline=60` (`scripts/deploy.sh`). Full writeup in `state-machine.md` §3.
  - Deployed and verified for real via a real self-addressed signed webhook through the actually-deployed pipeline: real extraction correctly classified `action_type="email"`, resolved the recipient, and drafted a real body with no falsely-required `due_at`; the real confirmation card showed the full draft; a real `Y` reply produced a real Gmail send (`obligations.action_type='email'`, `email_draft` matching what was shown, `email_sent_at` set, `items.type` still `obligation`, `items.state='COMMITTED'`) with zero exceptions from the real API call and zero entries in `dead_letters` this time — confirming the ack-deadline fix. Reading the sent message back via `users.messages.get` (the originally planned verification) turned out to need `gmail.readonly`, a scope never granted during step 6's bootstrap (only `gmail.send` was) — not worth a re-consent for one check, so verification relied on the real send call's own successful response instead, still fully real and non-mocked. All test DB rows cleaned up afterward.
- **Phase G — conversational redesign, user-directed after a real live test.** The user texted "hello" and saw two real problems: a ~2 minute reply, and a rigid Y/N confirmation card treating "hello" as a fake obligation (extractor-svc's schema had no "just chat" option — every message was forced into `type="obligation"`/`"latent"`). Full plan at `/Users/izaan/.claude/plans/spicy-purring-karp.md`, approved 2026-08-24: fix latency first (cheapest, unblocks fast iteration on everything after), then chat detection, then a new `messages` table for tone-mirroring, then a unified conversational Gemini call in `resolver-svc` replacing the rigid template/keyword-classifier flow — all while preserving ADR 0001 (state machine controls flow) and ADR 0003 (IAM-scoped, confirm-gated writes): the LLM's surface area can widen freely as long as it never gets a tool to trigger a write itself and `publish("items-confirmed", ...)` stays a plain pipeline-code check on a classified intent field.
  - **Step A (latency fix) — done.** `scripts/deploy.sh`: `--min-instances=1` for `ingest-svc`/`extractor-svc`/`resolver-svc` (the synchronous chat-reply path); `committer-svc`/`dispatcher-svc` left at 0 (not in that path). All three redeployed successfully.
  - Verified for real via signed webhooks against the live, now-warm services, polling the real Twilio message log rather than trusting the synchronous webhook ack alone (ingest-svc's ack is just the initial receipt, not the actual reply): a fresh message with no open conversation went `RECEIVED` → real Gemini extraction → real Gemini clarification call → real Twilio send in **~13 seconds** wall-clock, down from the previously observed ~2 minutes — roughly a 10x improvement. Real ongoing cost: 3 always-warm small Cloud Run instances.
  - **Honest scope note:** ~13s reflects a message that needed *two* real Gemini calls (extraction + clarification) plus two Pub/Sub hops — not yet the user's ~4-second target for a pure chat exchange like "yo wsg bro". That target is Step B's job (short-circuiting non-actionable messages to a single Gemini call with no clarification round), not something the latency fix alone reaches. Don't overstate this as "done, hits the 4s target" — it isn't, yet.
  - Testing this also re-surfaced the still-unfixed pre-existing bug Step B exists to fix: repeated test messages against the demo phone's already-open conversation state produced the same "💡 Respond to greeting" nonsense card seen in the original user report, and one `OTHER`-classified reply during a `DUPLICATE_SUSPECTED` state was silently swallowed (webhook 200'd, no SMS sent) — both are exactly the rigid-classifier/no-chat-path problems Phase G exists to replace, not new regressions from the latency change. All test conversation state cleared with a real `N` cancel before moving on.
- **Step B (chat detection) — done, fixes the exact "hello" bug the user hit.** Full design and findings in `agent-contracts.md` §2.2, migration `migrations/0006_add_chatted_state.sql` (new `CHATTED` terminal state, not folded into `CANCELLED`/`NEEDS_REVIEW` — different semantics, same reasoning `state-machine.md` already used once). `ExtractedItemMessage`/extractor-svc's schema gain `is_actionable`/`chat_reply`, verified against real Vertex AI in a scratch probe before any production code (6/6 real calls classified correctly — no repeat of step 10's `dict[str, Any]` failure, since this only widens field optionality). `resolver-svc`'s `/pubsub/push` short-circuits non-actionable items before the dedupe check, sends `chat_reply`, writes `CHATTED` — a `conversations` row is still written (empty) purely to keep the existing redelivery-idempotency guard working, not because there's a real exchange to track.
  - **Real bug hit during verification, not a Step B regression:** the very first live "hello" test after redeploying extractor-svc landed in `dead_letters` — the known ADK `InMemorySessionService` concurrent-redelivery race (first documented step 11, "fixed" via the step 15 ack-deadline bump), reproduced on the freshly-deployed revision's very first invocation before it was fully warm. A retry a few seconds later succeeded cleanly. Not re-fixed here — same accepted gap as before (extractor-svc can't have a DB idempotency guard, ADR 0003), just re-confirmed it can still bite right after a fresh deploy.
  - Deployed and verified for real: a real "hello" webhook produced a real `is_actionable=false` extraction and a real "hey! what's up?" SMS in ~11s, `items.state='CHATTED'`, `type`/`title` null — no fake obligation, no Y/N card. A follow-up real obligation message in the same session confirmed the actionable path (confirmation card, `N` cancel) is unchanged. 62/62 existing unit tests (extractor-svc + resolver-svc + shared) still pass unmodified — the new fields are additive/nullable, no existing test fixture broke.
- **Step C (`messages` table) — done, the tone-mirroring plumbing Step D will read from.** New migration `0007_messages_table.sql` — table + index + all three grants bundled in one file (matching migration 0001's own pattern for a brand-new table; grants only get split into their own file when adding access to an *existing* table, as in 0003-0005). Grants: `sa-ingest` INSERT-only (inbound), `sa-resolver`/`sa-dispatcher` SELECT+INSERT (both read history and log their own sends) — `sa-dispatcher` granted now rather than deferred to the Phase 2 follow-up, zero-cost in the same migration. `sa-extractor`/`sa-committer` get nothing (unchanged, confirmed no Twilio usage in committer-svc).
  - Code: `obligation_engine_shared.db.log_message()` (new, one shared implementation — reused by all three services rather than three copies that could drift). `ingest-svc` logs every inbound text right after resolving `user_id`, before the open-conversation/suggestion routing branches, so both fresh messages and replies get logged. `resolver-svc`/`dispatcher-svc`'s `_send_sms` now takes a leading `user_id` param and logs the outbound body in its own short transaction *after* the real Twilio send succeeds — decoupled from whatever transaction the caller is mid-way through, since the SMS really was sent regardless of what the caller's transaction does next. Every one of `_send_sms`'s 14 (resolver-svc) + 6 (dispatcher-svc) call sites updated to pass `user_id`, all of which already had it in scope (`extracted.user_id` or `payload.user_id`) — no new plumbing needed to thread it through.
  - 137/137 existing unit tests across resolver-svc/dispatcher-svc/ingest-svc/shared pass after updating ~20 test assertions that asserted on `_send_sms`'s old 2-arg call shape (`mock.call_args.args[1]` → `args[2]`, a few `assert_called_once_with(phone, ...)` → `assert_called_once_with(user_id, phone, ...)`) — purely mechanical, no test logic changed.
  - Deployed and verified for real: a fresh "yo whats good" produced a real inbound `messages` row and, once resolver-svc's real chat reply ("yo not much, what's up with you?") sent, a real outbound row — both directions confirmed via direct SQL against the live DB, correct `direction`/`body`/ordering.
- **Step D (unified conversational turn) — done, the actual redesign the user asked for.** New `resolver_svc/conversation.py`: one Gemini call (`converse()`) replaces `clarification.py`'s due_at-only clarify(), `templates.py`'s `render_confirmation_card`/`render_attached`/`render_cancelled`, and `reply_classifier.py`'s strict Y/N/ATTACH keyword matching — for everything except the dedupe question itself (`render_dedupe_question`/`render_merged` stay templated, deliberately out of scope — not what triggered the complaint). Still exactly two LLM call sites total (extraction, this). Combined schema (concrete field-merge + `Literal` intent classification `AFFIRM/DENY/CORRECTION/ATTACH/OTHER` + free-text `reply_text`) verified against real Vertex AI in a scratch probe before writing production code — 7/7 real scenarios validated correctly. `main.py`'s three reply-handling paths (`_start_clarification`, `_handle_clarification_reply`, the new `_handle_confirmation_reply`) all now run through `converse()`; the dedupe "N" branch (`_handle_duplicate_reply`) was also migrated onto the same mechanism rather than keeping a second parallel clarify()-based system alive for one path. New `messages`-table-backed tone-mirroring (`_recent_history`) feeds each call the user's own last 10 messages.
  - **ADR compliance, verified in the code, not just asserted:** the new `LlmAgent` has no `tools=[...]`. `publish("items-confirmed", ...)` fires on exactly one plain `if result.intent == "AFFIRM":` in `_handle_confirmation_reply` — never a second LLM call interpreting the first one's output. `CORRECTION` always merges into `resolved_fields` and stays `AWAITING_CONFIRMATION`, no matter how complete the result looks; verified for real below, not just by code review.
  - **Real bug #1, found during required verification, not by a test:** the very first live run of the user's own example flow ("yeo man got shit to do on friday at 12") got a real clarifying question ("what's the move on friday at 12?") instead of an immediate confirmation — `converse()` had added `"title"` to `still_missing` on its own initiative, a field name outside the only two the pipeline can ever merge (`due_at`/`email_recipient`), silently stranding the item asking about something it could never resolve. Fixed two ways: tightened the prompt to explicitly forbid any field name beyond those two, and added a defensive filter in `converse()` itself (the one choke point every caller goes through) that drops anything else from `still_missing` regardless of what the prompt produces — not trusting the prompt alone, per this project's established pattern of defense at the code layer, not just the instruction layer.
  - **Real design fix, found via a failing unit test, not live testing:** the original `/pubsub/push` handler did an extra `SELECT state FROM items` just to decide its own JSON response status after calling `_start_clarification()` — wasteful and fragile (broke the unit test's DB mock). Fixed by having `_start_clarification()` return its own outcome directly; no functional behavior changed, one fewer DB round trip.
  - Old tests for retired code deleted (`test_clarification.py`, `test_confirmation_card.py`); ~20 test assertions across `test_resolver.py`/`test_resolver_integration.py`/`test_dedupe_integration.py` updated to mock `converse()` instead of `clarify()`/`classify_reply`, plus two new tests (`test_correction_reply_never_auto_publishes`, and OTHER/ATTACH-with-no-candidate tests rewritten to reflect that unclear replies now get a natural response instead of the old silent no-op). 153/153 tests pass; `ruff check` clean.
  - Deployed and verified for real via three full live-SMS flows through the actually-deployed `ingest-svc`/`resolver-svc`, matching exactly what the plan required: (1) the user's own example, verbatim — "yeo man got shit to do on friday at 12" → real reply "bet, lock in 'something to do' on your calendar for friday at 12?" → "bet sounds good" → real reply "bet, locked it in for friday." → a real Calendar event (`state='COMMITTED'`, correct `due_at`, real `calendar_event_id`); (2) DENY — a fresh obligation, then "nah don't worry about it" → real reply "no worries, dropped it" → `state='CANCELLED'`, no Calendar write; (3) CORRECTION — a fresh obligation at 5pm, then "actually make it 3pm not 5pm" → real reply confirming the change and re-asking → **verified staying at `AWAITING_CONFIRMATION`, `due_at` correctly updated to 15:00 in `resolved_fields`, zero `obligations` row, zero `calendar_event_id`** — then a separate "yeah lock it in" → real Calendar event at the corrected 3pm time. All three real Calendar events and all test DB rows cleaned up afterward.
  - **Real infra hiccup along the way, unrelated to the code:** the local machine's disk filled to 0 bytes free mid-session, corrupting Docker Desktop's containerd blob store and hanging its daemon. Not something to fix blindly — flagged to the user, who freed space; Docker Desktop needed a full process kill + relaunch to recover cleanly (a plain restart-from-UI would likely have worked too, but the daemon was non-responsive to `docker ps` even after the disk issue cleared). Deploy resumed cleanly afterward with no code-side changes needed.
- **Conversation-continuity fix (Phase G follow-up, same session as step D) — done.** Real gap, found live during step D's own testing: `ingest-svc` routes any inbound SMS to whichever item a user has open (`DUPLICATE_SUSPECTED`/`CLARIFYING`/`AWAITING_CONFIRMATION`) purely by state, with no way to tell whether the message's *content* actually relates to that item — a stuck test item was absorbing unrelated follow-ups as if they answered its own pending question. Deliberately **not** fixed with a timeout (SMS threads are persistent on the user's own screen — a reply an hour or a day later is still often genuinely about the same item; only content, never elapsed time, can tell related from unrelated).
  - `resolver_svc/conversation.py`'s `ConversationTurnResult` gains `relates_to_item: bool = True` — a new "step 0" in the prompt, checked before field-merging/intent, defaulting true and forced true whenever there's no reply yet to judge (first turn). **Verified against real Vertex AI before wiring into the pipeline** (this project's established pattern): 9/9 real scenarios classified correctly, including the trickiest ones — a vague-but-related reply ("hmm not sure") stayed `True`, three genuinely unrelated replies (a new obligation, idle chat, an unrelated question) all correctly went `False`.
  - `main.py`: a new `_route_as_new_item()` helper — when a reply doesn't relate, the open item is left **completely untouched** (no state change, nothing lost, still there waiting) and the text gets its own brand-new item via a new shared `create_raw_item()` helper (`obligation_engine_shared/db.py`) + an `items-raw` publish, exactly the path a first-contact message takes. Wired into both `_handle_clarification_reply` and `_handle_confirmation_reply`. The `DUPLICATE_SUSPECTED` path's plain-keyword `classify_reply()` had the exact same bug in a different shape — its "neither Y nor N" branch used to **silently drop the reply with no SMS at all** (found reading that code while fixing the converse()-driven paths); given the same treatment rather than left as a worse, unrelated-seeming inconsistency.
  - **Two real infra gaps found only by live-testing, not anticipated up front — both fixed:** (1) `sa-resolver`'s DB grant never included `INSERT` on `items` (it only ever mutated rows `ingest-svc` had created, never originated one) — 500'd with `permission denied for table items` on the very first live unrelated-reply test; fixed via `migrations/0008_grant_resolver_items_insert.sql`. (2) `sa-resolver` had no Pub/Sub publisher IAM on `items-raw` (only `sa-ingest` did, per `infra/pubsub.tf`) — 500'd with `IAM_PERMISSION_DENIED` on the retry; fixed via a new `google_pubsub_topic_iam_member.resolver_publishes_raw` Terraform resource, planned (clean, one resource, nothing else drifted) and applied for real.
  - 3 new tests (`test_unrelated_reply_during_clarification_routes_as_new_item`, `test_unrelated_reply_during_confirmation_routes_as_new_item`, `test_duplicate_reply_outside_yn_routes_as_new_item`) plus the full existing suite: 41/41 passing for real against the live Pub/Sub emulator + Cloud SQL proxy (not just the 8 emulator-gated integration tests skipping quietly — actually run, this time, with `PUBSUB_EMULATOR_HOST`/`DB_USER` set). `ruff check` clean (no new violations; one pre-existing unrelated line-length warning in `db.py` left untouched).
  - **Deployed and verified for real** via five live-SMS flows through the actually-deployed pipeline, each confirmed against real DB state: (1) an unrelated message during `AWAITING_CONFIRMATION` (dentist item) spawned its own independent item (dry-cleaning) that reached its own real confirmation and, separately, its own real Calendar event — dentist item untouched throughout; (2) once the newer (dry-cleaning) item resolved to a terminal state, a bare "yes" correctly fell back to resuming the still-open dentist item — proving the fix doesn't break normal multi-thread recency ordering; (3) an unrelated chat message during `CLARIFYING` (expense report, still missing `due_at`) spawned its own `CHATTED` item with a real in-voice reply, while the expense item stayed `CLARIFYING`, untouched, and was later resumed and confirmed normally; (4)/(5) `DUPLICATE_SUSPECTED` (passport renewal): a genuine Y/N-unparseable reply — previously silent — now spawns its own item (haircut) that reached its own confirmation, while the dedupe question stayed untouched. All real Calendar events (4) and all test DB rows cleaned up afterward.
